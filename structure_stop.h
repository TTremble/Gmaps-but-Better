#include <stdio.h>
#include <stdlib.h>
struct Stop
{
    unsigned short num_arret;
    char* nom_Arret = NULL;
    nom_Arret = malloc(sizeof(int));
    if (memoireAllouee == NULL) // Si l'allocation a échoué
    {
        exit(0); // On arrête immédiatement le programme
    };
    char adresse[1000];
    
    int age;
    int genre; // Booléen : 1 = garçon, 0 = fille
};