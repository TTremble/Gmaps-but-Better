#include<stdio.h>
#include<stdlib.h>

int prochains(station A,station * reseau,int time){
  for(int i=0;i<=25479;i++){
    if(station->arete[i]!=0){
      printf("%s ",reseau[i]->name);
      printf("%i\n",arete[i]+time);
    }
  }
  return 0;
}
